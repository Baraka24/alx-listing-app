/** @type {import('tailwindcss').Config} */
module.exports = {
  // Tailwind CSS v4 no longer requires the `content` option. If you need to
  // customize theme or plugins, add them below. The project is already using
  // the v4 PostCSS plugin ("@tailwindcss/postcss").
  theme: {
    extend: {},
  },
  plugins: [],
};
